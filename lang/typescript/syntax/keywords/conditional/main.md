# TypeScript syntax - conditional

## Abstract
Conditional types in typescript look like this:

```typescript
T extends U ? x : Y
```
This means:
- If T is assignable to U, then the type resolves to X.
- Otherwise, it resolves to Y.

It's like an if statement for types

## Directory

## Useful Links

## Tags
